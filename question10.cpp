/*
The function/method sumDigits is intended to return the sum of digits of a given integer n.  
The code compiles but fails to produce the correct result due to incomplete loop logic.  

Example:
Input: 123
Output: 6
*/

#include <iostream>
using namespace std;

int sumDigits(int n) {
    int sum = 0;
    while (n > 0) {
     return sum;
    }   
}

int main() {
    int n = 765;
    cout << sumDigits(n) << endl;
    return 0;
}
